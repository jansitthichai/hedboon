# === Server secrets (Vercel / local Vite middleware) — DO NOT use VITE_ prefix ===
OPENAI_API_KEY=
GEMINI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
GEMINI_MODEL=gemini-2.0-flash
EMBEDDING_MODEL=text-embedding-3-small
RAG_TOP_K=5

# Legacy browser keys are deprecated and unused by RAG API.
# Keep empty. Do not put secrets in VITE_* for production.
# VITE_OPENAI_API_KEY=
# VITE_GEMINI_API_KEY=
